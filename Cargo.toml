# Its own workspace root, so this repository builds on its own.
[workspace]

[package]
name = "xmip-core-stream"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
repository = "https://github.com/IlleNilsson/xmip-core-stream"
description = "Immutable Stream model for Xmip."

[dependencies]
xcore = { package = "xmip-core", git = "https://github.com/IlleNilsson/xmip-core", branch = "main" }